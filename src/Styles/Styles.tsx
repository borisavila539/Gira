import { StyleSheet } from "react-native";

const styleGlobal = StyleSheet.create({
    Buttons: {
        width: '100%', 
        marginTop: 10, 
        alignItems: 'center'
    }
})

export {styleGlobal}